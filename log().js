const base = document.querySelector('')
const expo = document.querySelector('')
const text = document.querySelector('')

text.innerHTML = `${base} <sup>${}</sup> = ${expo}`
text.innerHTML = `log<sub>${base}</sub>(${argument}) = ${result}`